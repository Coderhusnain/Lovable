name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  noop:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Minimal success
        run: |
          echo "CI noop job — passing to clear status checks"
